version: 2.1
setup: true

orbs:
  path-filtering: circleci/path-filtering@1.1.0

workflows:
  setup-workflow:
    jobs:
      - path-filtering/filter:
          # Compare against the parent commit instead of a branch
          base-revision: HEAD~1
          config-path: .circleci/continue.yml
          mapping: |
            .* run-tests true
            .* run-build-deploy true